const immerPlugin = (Immer) => (store) => {
  const _setState = store.setState;
  store.setState = (input) => {
    if (input instanceof Function) {
      store.state = Immer.produce(input);
      store.runListeners();
    } else {
      _setState(input);
    }
  };
};
